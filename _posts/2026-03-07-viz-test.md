---
layout: post
title: "viz test: plotly integration"
date: 2026-03-07
categories: blog
hidden: true
custom_js: [plotly-charts]
---

Hidden test page for verifying Plotly chart integration. Three test cases below: external JSON, inline JSON, and a 2D bar chart.

## Test 1: 3D Point Cloud (external JSON)

Loading spec from `/assets/data/test-pointcloud.json`. You should see a 3D scatter plot with three groups: old-sonnet held (green), old-sonnet breached (red), new-sonnet held (cyan). Rotate, zoom, hover for metadata.

<div class="plotly-chart" data-chart="/assets/data/test-pointcloud.json"></div>

## Test 2: Horizontal Bar Chart (external JSON)

Loading spec from `/assets/data/test-barh.json`. D-Width by injection strategy. Hover shows fetches and refusal rate.

<div class="plotly-chart" data-chart="/assets/data/test-barh.json"></div>

## Test 3: Inline JSON spec

Spec embedded directly in the page via `<script data-for="...">`. Simple 2D scatter to test the inline pattern.

<div class="plotly-chart" id="inline-test"></div>
<script type="application/json" data-for="inline-test">
{
  "data": [
    {
      "type": "scatter",
      "mode": "markers",
      "name": "behavioral width",
      "x": ["progressive", "rescue", "breadcrumb", "breadcrumb_poison", "base64"],
      "y": [0.0, 0.0, 42.64, 3.92, 83.6],
      "marker": {
        "size": 12,
        "color": ["#ff79c6", "#ff79c6", "#50fa7b", "#f1fa8c", "#ffb86c"]
      },
      "text": ["dead", "dead", "breakthrough!", "payload killed lure", "largest shift"],
      "hoverinfo": "text+y"
    }
  ],
  "layout": {
    "title": {"text": "Inline Spec Test: D-Width by Strategy"},
    "yaxis": {"title": "D-Width"},
    "paper_bgcolor": "#282a36",
    "plot_bgcolor": "#282a36",
    "font": {"color": "#f8f8f2"},
    "margin": {"t": 40, "b": 40}
  }
}
</script>

## Checklist

- [ ] 3D scatter renders with rotation/zoom/pan
- [ ] Hover tooltips show trajectory metadata
- [ ] Legend toggles visibility of traces
- [ ] Horizontal bar chart renders correctly
- [ ] Inline JSON spec renders correctly
- [ ] Dark mode: charts display with correct colors (counter-inverted)
- [ ] Responsive: charts resize on window change
- [ ] No console errors
